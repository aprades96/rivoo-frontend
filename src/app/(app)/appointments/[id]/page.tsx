export default function AppointmentDetailPage({ params }: { params: Promise<{ id: string }> }) {
  return (
    <div className="p-4">
      <h1 className="text-lg font-semibold">Detalle de cita</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        En desarrollo
      </p>
    </div>
  )
}
